{
  "name": "geeklabs-td",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test",
    "preview": "vite preview",
    "deploy": "./scripts/deploy.sh"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
